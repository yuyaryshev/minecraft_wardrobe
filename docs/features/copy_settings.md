Copy settings feature

Add to Settings dialog:
- "Copy all setups" - should copy all Wardrobe settings as JSON
- "Copy all 'Current Setup'" - should copy current Wardrobe setup settings as JSON
- "Paste setup" - should paste setup from clipboard - must autodetect if that is one or all setups
