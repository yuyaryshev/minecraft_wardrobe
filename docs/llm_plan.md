# LLM Plan

Status values: not started -> not tested by llm -> not tested by human -> finished

- Wardrobe block (2 blocks tall, NBT persistence on drop/place): not tested by llm
- WardrobeConnection block and input scan (up to 16 connections): not tested by llm
- Recipes and loot tables for Wardrobe + Connection: not tested by llm
- Wardrobe UI layout with setup/operational modes and mode toggle: not tested by llm
- 8 setups with names and decorative icons: not tested by llm
- Setup mode slot binding/modes/min-max adjustments: not tested by llm
- Operational mode transfer logic (button + right click + single-slot): not tested by llm
- Operational highlights and output-full/error messages: not tested by llm
